import { MongoRepository } from '../../../../../src/contexts/shared/infrastructure/persistance/MongoRepository';

describe('constructor', () => {
	test('should some', () => {
		expect(true).toBe(true);
	});
});
